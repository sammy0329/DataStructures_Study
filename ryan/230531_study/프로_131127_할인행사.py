from typing import Dict
from collections import deque


def que2


def solution(want, number, discount):
    # dest: Dict = {key:number[idx] for idx, key in enumerate(want)}
    # now: Dict = {key:0 for idx, key in enumerate(want)}
    #
    # for order, each in enumerate(discount):
    #     if order < 10:
    #
    #     else:

    queue = deque(discount[:10])




    return
